-- 表領域の作成
 CREATE TABLESPACE SHIWAKE         -- 表領域名
 DATAFILE 'C:\APP\ORACLE\ORADATA\ORACLEKAIKEI\SHIWAKE.dbf' SIZE 300M  -- データファイル名
 AUTOEXTEND ON                      -- 自動拡張ON
 NEXT 100M MAXSIZE UNLIMITED;       -- 100Mずつ上限なし

-- KAIKEI表領域をデータファイルごと削除
-- DROP TABLESPACE SHIWAKE
-- INCLUDING CONTENTS AND DATAFILES 

-- ユーザーの作成
CREATE USER KAIKEIUSER
IDENTIFIED BY MANAGER -- パスワード
DEFAULT TABLESPACE SHIWAKE -- ユーザーが使用する表領域
QUOTA UNLIMITED ON SHIWAKE; -- ユーザーに割り当てる表領域

-- ユーザーをデータごと削除
-- DROP USER KAIKEIUSER CASCADE;

-- ユーザーに管理者権限を与える	
GRANT DBA TO KAIKEIUSER;

-- テーブルの作成
CREATE TABLE KAIKEIUSER.PEOPLE(
 id NVARCHAR2(10)
,name NVARCHAR2(50)
,mail NVARCHAR2(200)
,age NUMBER(2,0)
,memo NVARCHAR2(1000)
);
